{
  "name": "offline-word-recognition",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:smoke": "bash scripts/http_smoke.sh",
    "test:all": "npm test && npm run test:smoke"
  }
}